# QUALITY ROADMAP — AI Music Web 产品化路线

> 作者角色：音频产品架构师
> 目标：把当前前端 demo 升级为真正可用的音乐产品
> 核心优先级：Beat 质量 > Auto-Tune 适配度 > UI 设计

---

## 1. 当前前端 demo 已经完成了什么

### 1.1 用户交互闭环（Demo 级）

| 模块 | 已完成 | 说明 |
|------|--------|------|
| 音频上传 | 本地文件选择 + 播放 | 浏览器端 `FileReader` → `AudioBuffer` |
| 浏览器录音 | MediaRecorder 录制 + 回放 | 输出 WebM/Opus 格式 |
| 波形可视化 | Canvas 波形绘制 | 基于采样点的振幅包络 |
| 基础音频分析 | 时长、峰值、平均音量 | 时域统计 |
| 基础音高检测 | 自相关法 (autocorrelation) | 单音高估计，输出频率/音名/可信度 |
| 音准偏差分析 | 偏差 cents 计算 + 调性/音阶选择 | 与最近音阶音的距离 |
| 模拟 Auto-Tune | 前端数值模拟修正路径 | 仅展示修正方向和强度，不做真实音频处理 |
| Beat 风格选择 | 4 种风格的 UI 选择器 | 清爽电子 / 沉浸 Trap / 流行节奏 / 未来 R&B |
| Beat 规则推荐 | 基于音高+调性+音准的规则引擎 | 纯前端 if-else 规则 |
| Beat 浏览器预览 | Web Audio API 合成节奏 | Oscillator kick + noise snare/hihat，非 AI 生成 |
| 结果预览面板 | 汇总展示所有状态 | 上传/录音/Auto-Tune/Beat 四维状态 |

### 1.2 技术栈现状

- 纯前端单文件 HTML + CSS + JS
- 无第三方依赖
- Web Audio API (AudioContext, OscillatorNode, BiquadFilter)
- MediaRecorder API
- 音高检测：自相关法 (70-700Hz 范围)
- 音准分析：MIDI 映射 + 音阶匹配

### 1.3 Demo 的边界（明确不做的事）

- 不连接后端
- 不调用真实 AI 模型
- 不做真实音频修音（只做数值模拟）
- 不生成真实 Beat 音频文件
- 不做多轨混音

---

## 2. 哪些能力必须迁移到后端

以下能力在浏览器端有硬性限制，必须迁移到后端才能达到产品级质量：

### 2.1 必须后端化

| 能力 | 前端限制 | 后端方案 |
|------|----------|----------|
| **真实音高修正 (Auto-Tune)** | Web Audio 无法做相位声码器级别的实时修音 | Python/C++ 后端处理，输出修正后的音频文件 |
| **AI Beat 生成** | 浏览器跑不动扩散模型/Transformer | GPU 服务器跑 MusicGen / Stable Audio / 自定义模型 |
| **多轨混音** | 前端只能做简单增益叠加 | 后端专业混音引擎（音量平衡、EQ、压缩、混响） |
| **人声分离** | 前端算力不够 | Demucs / Spleeter / MDX-Net 后端推理 |
| **高质量母带处理** | 前端无能力 | 后端自动化母带链 (multiband compression, limiting) |
| **音频格式转换** | 浏览器编码格式有限 | 后端 FFmpeg / libsndfile 全格式支持 |
| **用户数据持久化** | 只能存 localStorage | 数据库 + 对象存储 (S3/OSS) |
| **模型权重保护** | 前端暴露所有代码 | 模型权重留在服务端，前端永远拿不到 |

### 2.2 可以保留在前端的

| 能力 | 原因 |
|------|------|
| 录音 | MediaRecorder 足够，传 PCM/WebM 到后端即可 |
| 波形可视化 | 纯展示，Canvas 完全胜任 |
| 音频播放 | `<audio>` 标签 + 后端返回的处理后音频 URL |
| UI 交互 | 参数调节、风格选择、状态展示 |
| 基础的客户端校验 | 文件格式、大小检查 |

---

## 3. 真实 Auto-Tune 需要哪些模块

真正的 Auto-Tune 不是"把音高拉到最近音阶音"这么简单。以下是产品级 Auto-Tune 的完整模块拆解：

### 3.1 核心处理链

```
输入音频 → [1] → [2] → [3] → [4] → [5] → [6] → 输出音频
```

| 编号 | 模块 | 职责 | 关键算法 |
|------|------|------|----------|
| **1** | **基频检测 (F0 Estimation)** | 逐帧提取人声基频 | pYIN / CREPE / DIO / SWIPE |
| **2** | **有声/无声判断 (VAD)** | 只修有声音的帧，静音帧保持原样 | 能量阈值 + 基频可信度 |
| **3** | **目标音高映射 (Pitch Mapping)** | 把检测到的基频映射到目标音阶 | 音阶快照 (scale snap) + 用户选择的调性/音阶 |
| **4** | **修音强度控制 (Correction Strength)** | 0%=不修，100%=完全拉到目标音 | retune speed 参数 + 线性插值 |
| **5** | **相位声码器 (Phase Vocoder)** | 在不改变时长/共振峰的前提下修改音高 | STFT → 相位调整 → ISTFT |
| **6** | **共振峰保持 (Formant Preservation)** | 修音后保持人声自然度，不会变成"花栗鼠" | 共振峰位移补偿 / PSOLA |

### 3.2 关键参数（用户可调）

| 参数 | 含义 | 范围 |
|------|------|------|
| Retune Speed | 多快把音拉到目标 | 0 (慢/自然) ~ 100 (快/电子感) |
| Scale / Key | 目标调性 | 12 个大调 + 12 个小调 |
| Formant Shift | 共振峰偏移（改变音色性别感） | -12 ~ +12 半音 |
| Humanize | 保留多少自然颤音/滑音 | 0 ~ 100 |
| Flex Pitch Range | 允许修正的最大音高跨度 | ±1 ~ ±12 半音 |

### 3.3 技术选型建议

- **Python 后端**：`pyworld` (F0) + `librosa` (STFT/ISTFT) + `pyrubberband` (相位声码器)
- **或 C++ 高性能方案**：`World` 声码器 + `Rubber Band Library`
- **云端 GPU 方案**：如果需要极致质量，用神经网络声码器 (如 HiFi-GAN 做重合成)

---

## 4. 高质量 Beat 生成需要哪些模块

### 4.1 前端 demo 的 Beat 是什么

当前 demo 的 Beat 本质上是用 Web Audio API 的振荡器 + 噪声合成的 8 步鼓机：
- Kick：正弦波从 140Hz 滑到 50Hz
- Snare：带通滤波白噪声
- HiHat：高通滤波白噪声
- 4 种固定节奏型 (kick/snare/hat 的 8-step pattern)

这不是"生成"，是手工编写的鼓机音序。

### 4.2 产品级 Beat 生成的三个层次

#### 层次 A：采样鼓机（低门槛起步）

```
风格模板 → 预置 MIDI pattern → 高质量鼓采样 → 混音 → WAV 输出
```

- 需要：专业鼓采样库 (kick/snare/hihat/808/clap/percs)
- 每种风格 4-8 个 pattern 变体
- 对采样做音量包络 + 轻微随机化避免机械感
- **优点**：实现快，质量稳定，不需要 GPU
- **缺点**：不是真正"生成"，是模板填充

#### 层次 B：AI 辅助编曲（中等门槛）

```
人声分析 → 风格理解 → 和弦进行生成 → 鼓 pattern + Bassline + 和声填充
```

- 和弦生成：用 Music Transformer / ChordGAN 根据人声调性生成 progression
- Bassline：跟随和弦根音 + 节奏模板
- 鼓 pattern：VAE 生成或规则+随机组合
- 需要 GPU (T4/A10 级别)

#### 层次 C：端到端 AI 生成（高门槛）

```
人声输入 → MusicGen / Stable Audio / Suno-like 模型 → 完整伴奏音频
```

- 模型：Meta MusicGen / Stability Stable Audio / 自训练扩散模型
- 需要 A100/H100 级别 GPU
- 可控性差（生成结果不可预测）
- **当前不推荐作为主要方案**，可作为"灵感模式"

### 4.3 推荐起步路径

**v2.0 建议从层次 A + B 的混合方案开始**：

1. 用高质量鼓采样 + MIDI pattern 模板保证节奏质量
2. 用轻量 AI 模型生成和弦进行和 Bassline
3. 后端合成 + 混音后返回完整伴奏 WAV

### 4.4 Beat 质量的核心瓶颈

| 瓶颈 | 影响 | 解决方案 |
|------|------|----------|
| 鼓采样质量 | 决定 Beat 听感的第一印象 | 采购/自制专业采样包 |
| 节奏型多样性 | 同一风格不能来来回回一种 pattern | 模板库 + 规则变体引擎 |
| Swing/Groove | 没有律动感的 Beat 听起来"死的" | MIDI 时序偏移 + 力度随机化 |
| 混音 | 各声部打架，鼓被埋掉 | 多轨混音：EQ + 侧链压缩 + 母带 |
| 和弦适配度 | 和声和人声打架 | 和弦检测 + 调性约束 |

---

## 5. 推荐技术路线

### v2.0 — 后端 MVP（目标：听到真实修音 + 听到真实 Beat）

**时间估算**：4-6 周（单人全职）

| 模块 | 内容 | 技术 |
|------|------|------|
| 后端框架 | REST API 服务 | FastAPI (Python) 或 Express (Node.js) |
| 真实修音引擎 | 基频检测 + 相位声码器修音 | pyworld + librosa / pyrubberband |
| Beat 生成引擎 | 采样鼓机 + 模板 pattern | 鼓采样库 + MIDI pattern JSON 模板 |
| 混音引擎 | 人声 + Beat 叠加 + 基础 EQ | pydub / sox 后端处理 |
| 文件传输 | 前端上传 → 后端处理 → 返回下载链接 | multipart upload + 临时 URL |
| 前端改造 | 去掉模拟态，接入真实 API | fetch + 处理中 loading 状态 + 结果播放 |

**v2.0 完成标准**：
- 用户上传人声 → 选择调性/音阶 → 听到真实修音后的音频
- 用户选择风格 → 听到鼓采样 + Bassline 的伴奏
- 一键混合：下载人声+伴奏的混合 WAV

### v2.1 — 修音体验升级（目标：专业级 Auto-Tune 手感）

**时间估算**：3-4 周

| 模块 | 内容 |
|------|------|
| Retune Speed 实时调节 | 不再是百分比滑块，而是真正的修音速度参数 |
| 共振峰保持 | 修音后人声不变"花栗鼠"味 |
| Humanize 颤音保留 | 检测自然颤音，不被修掉 |
| 音阶编辑器 | 用户自定义哪些音属于目标音阶（不只大调/小调） |
| A/B 对比播放 | 一键切换原声 vs 修音后 |
| MIDI 导引模式 | 用户上传一段 MIDI 旋律，人声强制对齐到 MIDI 音高 |

### v2.2 — Beat 质量飞跃（目标：不输入门 DAW 模板的伴奏）

**时间估算**：4-6 周

| 模块 | 内容 |
|------|------|
| 专业鼓采样库 | 多层次力度采样 (multi-velocity) |
| 风格模板扩展 | 从 4 种扩展到 20+ 种风格 |
| AI 和弦生成 | 基于人声调性 + 情感分析生成和弦进行 |
| 自动 Bassline | 跟随和弦根音 + 节奏同步 |
| 和声填充 | Pad / Pluck / Arp 等声部（可选） |
| 侧链压缩 | Kick 触发闪避，做出"泵感" |
| 母带处理链 | 多段压缩 + 限制器 + 响度标准化 |

### v3.0 — 智能创作助手（目标：一人完成完整歌曲）

**时间估算**：8-12 周

| 模块 | 内容 |
|------|------|
| 人声分离 | 用户上传含伴奏的人声，自动分离 |
| 歌词到旋律 | 输入歌词 → 生成参考旋律 → 用户演唱 → 修音 |
| 结构编排 | 自动建议 Intro/Verse/Chorus/Bridge/Outro |
| 多风格融合 | 一首歌里切换多种 Beat 风格 |
| 导出分轨 | 导出多轨 WAV，可导入 FL Studio 等 DAW 精修 |
| 协作功能 | 分享项目链接，多人在线协作 |

### 后续扩展：FL Studio / MIDI 集成（记录但不开发）

以下能力是自然的产品延伸，但在 v3.0 之前不投入开发资源：

- MIDI 导出：将生成的和弦进行/Bassline 导出为 MIDI 文件
- FL Studio 插件：以 VST/AU 插件形式嵌入，DAW 内直接调用修音和伴奏生成
- MIDI 控制器映射：用 MIDI 键盘控制修音参数、触发 pattern 切换
- Ableton Link 同步：与其他音乐软件节拍同步

---

## 6. 哪些核心算法不能放在公开前端代码里

现在 `index.html` 里所有代码都是公开可见的。产品化后，以下算法/数据必须留在后端：

### 6.1 绝对不可公开

| 内容 | 原因 |
|------|------|
| **修音模型权重** | 自训练的 F0 检测模型 / 声码器模型是核心壁垒 |
| **Beat 生成模型权重** | MusicGen / 自训练扩散模型的 checkpoint |
| **鼓采样库** | 购买/自制的采样包有版权和商业价值 |
| **和弦生成模型** | 训练数据和权重都是竞争壁垒 |
| **风格模板库** | 精心调教的 pattern + 参数组合是产品 know-how |
| **母带处理链参数** | 多段压缩阈值/比例/增益是调音经验结晶 |

### 6.2 可以公开

| 内容 | 原因 |
|------|------|
| 前端 UI 代码 | 交互逻辑没有壁垒 |
| 波形可视化算法 | 通用技术，任何人都能写 |
| API 接口定义 | 公开文档有助于生态建设 |
| 录音/播放逻辑 | 浏览器标准 API |

### 6.3 灰色地带（建议不公开）

| 内容 | 原因 |
|------|------|
| 音高检测参数 | 当前自相关法的窗长、步长、阈值是调出来的 |
| 音阶映射规则 | 虽然简单但属于产品逻辑 |
| Beat 推荐规则 | 从 demo 的 if-else 升级后的规则引擎 |

---

## 7. 后续如何保护核心创意和算法

### 7.1 代码层面

| 措施 | 说明 |
|------|------|
| **前后端分离** | 前端仓库只放 UI 代码，后端仓库私有 |
| **模型权重加密** | 部署时用 AES 加密权重文件，运行时解密加载到内存 |
| **API 鉴权** | JWT + API Key，防止未授权调用 |
| **速率限制** | 防止通过大量请求逆向工程 |
| **水印嵌入** | 生成的音频嵌入不可听水印，追溯泄露来源 |

### 7.2 产品层面

| 措施 | 说明 |
|------|------|
| **SaaS 模式** | 核心能力永远在云端，用户拿到的是处理结果，不是处理引擎 |
| **免费层限制** | 免费用户可用基础功能，高级修音/Beat 生成收费 |
| **输出水印** | 免费版生成的音频加可听水印/周期性静音 |
| **竞业条款** | 如与外部开发者合作，签署 NDA + 竞业限制 |

### 7.3 法律层面

| 措施 | 说明 |
|------|------|
| **软件著作权** | 注册后端核心引擎的软件著作权 |
| **商标注册** | 产品名称 + Logo 商标 |
| **训练数据合规** | 如果用外部数据训练模型，确保有合法授权 |
| **采样版权** | 鼓采样库需确认商业使用授权 |

---

## 附录：当前 Demo 到 v2.0 的关键差异对比

| 维度 | 当前 Demo (v0.9) | v2.0 目标 |
|------|------------------|-----------|
| Auto-Tune | 前端数值模拟，不改变音频 | 后端真实修音，下载修音后 WAV |
| Beat 生成 | 浏览器振荡器合成鼓机 | 专业采样 + MIDI pattern + Bassline |
| 混合输出 | 不支持 | 人声 + 伴奏混音后单文件下载 |
| 音高检测 | 自相关法，单音高，70-700Hz | pYIN/CREPE，多音高候选，更高精度 |
| 处理延迟 | 0（纯前端模拟） | 3-15 秒（取决于音频长度和模型） |
| 架构 | 单文件 HTML | 前后端分离 |
| 模型保护 | 无（全部代码可见） | 权重/参数全在后端 |
