# AI Workflow for AI Music Web

这是一个面向新手的项目协作流程文档，记录如何让 AI 扮演不同角色、完成项目各阶段工作。

## 1. 项目启动阶段：产品经理 AI 收敛 MVP

**目的**：明确产品目标、范围和版本边界。

提示词模板：
```
你是一名产品经理。我们正在做一个前端音乐工具 MVP。
请帮我收敛当前版本的目标、核心功能和不做范围。
输出：
1. 版本目标一句话
2. 当前必须做的功能列表
3. 当前不包含的内容列表
4. 版本完成标准
```

## 2. UI 设计阶段：UI 设计师 AI 输出页面方案

**目的**：定义页面结构、交互和展示内容。

提示词模板：
```
你是一名 UI 设计师。请基于以下功能需求，给出一个单页面结构和主要区域布局。
需求：上传音频、浏览器录音、波形可视化、音高检测、Beat 推荐。
输出：
1. 页面主要区块
2. 每个区块的标题和简要说明
3. 关键交互控件
4. 样式调性建议（简洁、黑暗、音乐工作台）
```

## 3. 开发阶段：前端/后端工程师 AI 按范围写代码

**目的**：让 AI 按指定范围实现功能，避免越界。

提示词模板：
```
你是一名前端工程师。当前项目只修改 `index.html`，不引入第三方库，不接后端。
请实现以下功能：
- 浏览器音频上传和播放
- 录音并播放
- 波形可视化
- 音高检测和偏差分析
- 规则化 Beat 推荐
请输出最终的 `index.html` 文件内容，并保留现有样式。
```

如果有后端需求：
```
你是一名后端工程师。请设计一个简单 API 接口说明，用于接收音频分析结果和返回推荐配置。
不要写前端代码，只输出接口定义和示例请求/响应。
```

## 4. 测试阶段：测试员 AI 找必须修改的问题

**目的**：检查功能、边界和值得修复的问题。

提示词模板：
```
你是一名测试员。请检查当前页面功能是否符合以下要求：
- 上传、录音、播放、波形、音高检测、音准分析、模拟修正、Beat 推荐
- 没有稳定音高时显示正确提示
- 推荐结果不误导为真实生成

请只返回：
- 必须修改的问题
- 可以以后再做的问题
- 不需要改的问题
```

## 5. 修复阶段：工程师 AI 只修指定问题

**目的**：让 AI 精准修复测试发现的问题，不做额外改动。

提示词模板：
```
你是一名前端工程师。当前任务是修复以下问题：
- 说明问题1
- 说明问题2

请只修改与这些问题相关的代码，不要增加新功能。
输出修改后的文件内容或说明具体改动位置。
```

## 6. 发布阶段：用 Git / GitHub / GitHub Pages 保存和发布项目

**目的**：把项目变成可复用的版本，并部署到网页。

简单流程：
1. `git init`
2. `git add .`
3. `git commit -m "v0.9 完成"`
4. `git branch -M main`
5. 在 GitHub 创建仓库并复制仓库地址
6. `git remote add origin <repo-url>`
7. `git push -u origin main`

如果要发布到 GitHub Pages：
1. 在仓库设置里打开 GitHub Pages
2. 选择 `main` 分支 / `root` 目录
3. 保存后等待页面地址生效

## 7. 小结

这个流程重点是：
- 每个阶段都用 AI 扮演单一角色
- 用清晰的提示词告诉 AI“做什么”和“不做什么”
- 让测试阶段输出“必须修改的问题”
- 修复阶段只按问题修，不要扩展功能
- 发布阶段保持简单，先把代码保存到 GitHub，再考虑 Pages 部署

> 这个文件适合以后直接复用，只要把提示词里的项目名和需求替换成当前工作内容即可。